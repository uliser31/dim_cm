<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="dim_cm.list">
      <field name="name">dim_cm list</field>
      <field name="model">dim_cm.dim_cm</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="dim_cm.action_window">
      <field name="name">dim_cm window</field>
      <field name="res_model">dim_cm.dim_cm</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="dim_cm.action_server">
      <field name="name">dim_cm server</field>
      <field name="model_id" ref="model_dim_cm_dim_cm"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="dim_cm" id="dim_cm.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="dim_cm.menu_1" parent="dim_cm.menu_root"/>
    <menuitem name="Menu 2" id="dim_cm.menu_2" parent="dim_cm.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="dim_cm.menu_1_list" parent="dim_cm.menu_1"
              action="dim_cm.action_window"/>
    <menuitem name="Server to list" id="dim_cm" parent="dim_cm.menu_2"
              action="dim_cm.action_server"/>
-->
  </data>
</odoo>